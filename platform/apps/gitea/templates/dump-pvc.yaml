{{- if .Values.backupExportJob.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.dumpPvc.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    app.kubernetes.io/managed-by: gitea-backup
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.dumpPvc.size }}
{{- end }}
